<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ssg.echodairy.mapper.DiaryMapper">

    <!-- 신규 저장 -->
    <insert id="insertDiary">
        INSERT INTO diary (
            user_id,
            content,
            emotion,
            diary_date
        ) VALUES (
                     #{userId},
                     #{req.content},
                     #{req.emotion},
                     #{req.diaryDate}
                 )
    </insert>

    <!-- 날짜 기준 조회 -->
    <select id="findDiaryByDate"
            resultType="com.ssg.echodairy.dto.TodayRequest">
        SELECT
            content,
            emotion,
            diary_date AS diaryDate
        FROM diary
        WHERE user_id = #{userId}
          AND diary_date = #{diaryDate}
            LIMIT 1
    </select>

    <!-- 수정 -->
    <update id="updateDiary">
        UPDATE diary
        SET
            content = #{req.content},
            emotion = #{req.emotion}
        WHERE user_id = #{userId}
          AND diary_date = #{req.diaryDate}
    </update>

    <!-- ⭐ AI 전용 조회 -->
    <select id="findDiaryForAi"
            resultType="com.ssg.echodairy.dto.DiaryAiDto">
        SELECT
            diary_id AS diaryId,
            user_id  AS userId,
            content,
            emotion
        FROM diary
        WHERE user_id = #{userId}
          AND diary_date = #{diaryDate}
            LIMIT 1
    </select>



</mapper>

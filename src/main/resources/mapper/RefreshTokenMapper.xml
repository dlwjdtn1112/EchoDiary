<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ssg.echodairy.mapper.RefreshTokenMapper">

    <insert id="save">
        INSERT INTO refresh_token (
            user_id,
            refresh_token,
            expires_at
        )
        VALUES (
                   #{userId},
                   #{refreshToken},
                   #{expiresAt}
               )
    </insert>

    <select id="findUserIdByToken" resultType="long">
        SELECT user_id
        FROM refresh_token
        WHERE refresh_token = #{refreshToken}
    </select>

    <delete id="deleteByUserId">
        DELETE FROM refresh_token
        WHERE user_id = #{userId}
    </delete>

</mapper>
